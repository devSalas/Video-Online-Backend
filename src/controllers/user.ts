import { Resquest , Response} from "express"
import { createUser } from "../services/user"
import { User } from "../models/user" 
const createUser =async (req:Resquest,res:Response)=>{

   try {
    
   } catch (error) {
    
   }

}

export {createUser}